node_modules/
dist/
.astro/
.wrangler/
.env
.env.*
!.env.example
# Cloudflare local-dev secrets. `.env.*` does not match this name, and the file is
# documented in CLAUDE.md as a place to put SUPABASE_URL and SUPABASE_KEY.
.dev.vars
.dev.vars.*
coverage/
playwright-report/
test-results/
playwright/.auth/
.DS_Store

# Claude Code project configuration is committed. A global core.excludesFile may ignore
# .claude/ wholesale, so re-include it here and keep only personal overrides out.
!.claude/
.claude/settings.local.json
